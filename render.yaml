services:
  - type: web
    name: tic-tac-toe-webapp
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    rootDir: tic_tac_toe_webapp
